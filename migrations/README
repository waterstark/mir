# для того, что бы сделать миграции со всеми нужными параметрами,
нужно импортировать из файла params_choice списки, они интуитивно названы как и поля в модели,
и подставить их в параметры Choice_type
иначе alembic неправильно подставляет значения